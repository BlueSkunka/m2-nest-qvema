meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/in
  body: json
  auth: none
}

body:json {
  {
    "email": "test@mail.com",
    "password": "caribou"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("token",data.access_token);
}
